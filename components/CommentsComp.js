import React from "react";

const CommentsComp = () => {
  return <div>CommentsComp</div>;
};

export default CommentsComp;

// should be allowed to comment if you are logged in
// when you create a post to it to the auth uid,

// we are going to duplicate information in the user since we dont want to do 2 query searches for our data
